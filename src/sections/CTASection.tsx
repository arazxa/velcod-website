export function CTASection() {
  return (
    <section className="section cta">
      <h2>Ready to build?</h2>
      <p>Start iterating on sections and styles to match the final replica.</p>
      <button type="button">Get Started</button>
    </section>
  );
}
